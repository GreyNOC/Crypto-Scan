# Normalize line endings: store and check out text as LF on every platform.
* text=auto eol=lf

# Shell / CI / config that breaks with CRLF stays LF.
*.sh   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
